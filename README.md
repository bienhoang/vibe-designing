<div align="center">

# Vibe Designing

**Vibe Design meets Figma**

Let AI agents design directly in Figma through natural conversation. Read layouts, create components, modify styles, and build entire design systems with 50+ design tools.

[![MIT License](https://img.shields.io/badge/license-MIT-blue.svg)](./LICENSE)

</div>

---

## Table of Contents

- [How It Works](#how-it-works)
- [What You Can Do](#what-you-can-do)
- [Setup](#setup)
  - [Prerequisites](#prerequisites)
  - [Option 1: NPM (Zero-Download)](#option-1-npm-zero-download)
  - [Option 2: Build from Source](#option-2-build-from-source)
  - [About Ports](#about-ports)
  - [Connect](#connect)
  - [Troubleshooting](#troubleshooting)
  - [Instructions for AI Agents](#instructions-for-ai-agents)
- [Documentation](#documentation)
- [Common Tasks](#common-tasks)
- [Status](#status)
- [Community](#community)
- [License & Attribution](#license--attribution)

## How It Works

```
Claude/Cursor (AI)
  ↓ MCP Protocol
MCP Server (localhost)
  ↓ WebSocket
Relay (localhost:3055)
  ↓ WebSocket
Figma Plugin (your design file)
  ↓ Figma API
Your Design System
```

AI agents send design commands via MCP tools. Vibe Designing converts them to Figma operations in real-time. Changes appear instantly in your design file.

## What You Can Do

| Category | Tools | Examples |
|----------|-------|----------|
| **Create** | 16 | Frames, components, text, shapes, SVG imports, boolean ops |
| **Modify** | 17 | Move, resize, colors, text, effects, constraints, exports |
| **Design Systems** | 16 | Create styles, variables, collections, bindings |
| **Query** | 14 | Get node info, search, selection, serialize design |
| **Quality** | 1 | Lint designs, WCAG 2.2 validation, auto-fix |
| **Total** | **50+** | Cover creation to production |

See full [tool reference](./docs/codebase-summary.md#tool-categories-50-tools).

## Setup

### Prerequisites

- [Node.js](https://nodejs.org) (v18+)
- [Figma](https://figma.com) desktop or web app
- An AI coding tool with MCP support (Claude Code, Cursor, etc.)
- **Best LLM:** Claude Opus 4.6 (consistent results). GPT models less reliable.

### Option 1: NPM (Zero-Download)

No cloning, no building. Install the tunnel and MCP server from npm, grab the plugin from GitHub, and connect.

#### 1. Download the Figma plugin

Grab the latest **vibe-designing-plugin** from [GitHub Releases](https://github.com/bienhoang/vibe-designing/releases) — it contains `manifest.json`, `code.js`, and `ui.html`.

Unzip it.

#### 2. Start the WebSocket tunnel

```bash
npx @bienhoang/vibe-designing-tunnel
```

You should see: `WebSocket server running on port 3055`

#### 3. Install the Figma plugin

1. In Figma, go to **Plugins > Development > Import plugin from manifest...**
2. Select the `manifest.json` from the unzipped plugin folder
3. Run the plugin — it will show a connection panel

#### 4. Configure MCP in your AI tool

Add to your MCP config (e.g. `.cursor/mcp.json`, `.claude.json`, or `.mcp.json`):

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "npx",
      "args": ["-y", "@bienhoang/vibe-designing"]
    }
  }
}
```

If using a non-default port:

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "npx",
      "args": ["-y", "@bienhoang/vibe-designing", "--port=3056"]
    }
  }
}
```

Then continue to [Connect](#connect).

---

### Option 2: Build from Source

Full source control. Clone the repo and run everything locally.

```bash
git clone https://github.com/bienhoang/vibe-designing.git
cd vibe-designing
```

#### 1. Install dependencies

```bash
npm install
npm run build
```

#### 2. Start the WebSocket relay

```bash
npm run socket
```

You should see: `WebSocket server running on port 3055`

#### 3. Install the Figma plugin

1. In Figma, go to **Plugins > Development > Import plugin from manifest...**
2. Select `plugin/manifest.json` from this repo (generated by `npm run build`)
3. Run the plugin — it will show a connection panel

#### 4. Configure MCP in your AI tool

Add to your MCP config (e.g. `.cursor/mcp.json`, `.claude.json`, or `.mcp.json`):

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "node",
      "args": ["/absolute/path/to/vibe-designing/dist/mcp.js"]
    }
  }
}
```

Or run from source (no build step, good for development):

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "npx",
      "args": ["tsx", "/absolute/path/to/vibe-designing/src/server/mcp.ts"]
    }
  }
}
```

If using a non-default port, pass `--port=`:

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "node",
      "args": ["/absolute/path/to/vibe-designing/dist/mcp.js", "--port=3056"]
    }
  }
}
```

> **Note:** Replace `/absolute/path/to/vibe-designing` with the actual path to your cloned repo. Run `pwd` from the vibe-designing directory to find it. MCP clients don't set a working directory, so relative paths won't resolve.

Then continue to [Connect](#connect).

---

### About Ports

Vibe Designing defaults to port **3055**. The Figma plugin whitelists ports **3055–3058** so you have alternatives if 3055 is already in use. To use a different port:

**NPM:** `VIBE_DESIGNING_PORT=3056 npx @bienhoang/vibe-designing-tunnel`

**Source:** `VIBE_DESIGNING_PORT=3056 npm run socket`

The MCP server reads the same env var, so it will match automatically. Update the **port field in the Figma plugin UI** before clicking Connect.

### Connect

1. In the Figma plugin, set the channel name to `vibe-designing` (or any name you like)
2. Click **Connect**
3. In your AI tool, call `join_channel` with the same channel name (defaults to `vibe-designing`)
4. Call `ping` — you should get back `pong` with your document name

#### Channel rules

Each channel enforces **exactly one Figma plugin and one MCP server**. If a second plugin (or second MCP) tries to join a channel that's already occupied, it will be rejected with a clear error message. To connect a different plugin, disconnect the first one or use a different channel name.

#### Debugging connections (source build only)

```bash
curl http://localhost:3055/channels
```

Returns:
```json
{
  "vibe-designing": {
    "mcp": { "connected": true, "version": "0.1.1", "joinedAt": "2026-02-28T12:00:00.000Z" },
    "plugin": { "connected": true, "version": "0.1.1", "joinedAt": "2026-02-28T12:00:01.000Z" }
  }
}
```

If using Claude Code, the `channel_info` MCP tool returns the same data without leaving your AI tool.

### Troubleshooting

**Plugin won't connect to WebSocket**: Make sure the relay/tunnel is running. The plugin connects to `ws://localhost:3055` by default.

**"Connection rejected" in plugin UI**: Another plugin is already connected to that channel. Disconnect it first or use a different channel name. Run `curl http://localhost:3055/channels` to see what's connected.

**MCP shows disconnected**: Restart your AI tool after changing MCP config. Stdio-based MCP servers can't hot-reload.

**Port already in use**: Kill the existing process (`lsof -ti:3055 | xargs kill`) or use the next available port (3056–3058).

**Version mismatch warning** (source build): The plugin and MCP server are running different versions. Run `npm run build` and restart both to resolve.

---

### Instructions for AI Agents

The section below is for AI agents setting up Vibe Designing on behalf of a user. Follow these steps in order.

#### Plugin download (NPM setup only)

Download the latest **vibe-designing-plugin** zip from [GitHub Releases](https://github.com/bienhoang/vibe-designing/releases) and unzip it. After unzipping, tell the user the full path to the `manifest.json` so they can easily find it when importing the plugin in Figma.

#### Port resolution

Before starting the tunnel/relay, check if the default port is available:

```bash
lsof -ti:3055
```

If the port is occupied, ask the user:

> Port 3055 is already in use by another process (PID: <pid>). Would you like to:
> 1. **Kill the process** on port 3055 and use it for Vibe Designing
> 2. **Use the next available port** (3056, 3057, or 3058)

If the user chooses option 2, scan ports in order:

```bash
lsof -ti:3056 || echo "3056 is free"
lsof -ti:3057 || echo "3057 is free"
lsof -ti:3058 || echo "3058 is free"
```

Use the first free port. Set `VIBE_DESIGNING_PORT=<port>` when starting the tunnel/relay, and pass `--port=<port>` to the MCP server in the MCP config. Inform the user to set the same port in the Figma plugin UI before clicking Connect.

If all four ports (3055–3058) are occupied, tell the user they need to free one.

#### Connection verification

After the user opens the Figma plugin, it should automatically show **Connected** on the default port (3055). If a non-default port was used, the user will need to select the correct port in the plugin UI and click Connect.

1. Call `join_channel` (defaults to channel `vibe-designing` — use a different name only if the user specifies one).
2. Call `ping`. Expected response: `{ status: "pong", documentName: "...", currentPage: "...", timestamp: ... }`

If `ping` returns a `pong` with a document name, the full chain is verified. Proceed with design tasks.

#### Troubleshooting connection issues

If the plugin shows **Disconnected** on port 3055, try the following before asking the user:

1. Check the tunnel/relay is running: `lsof -ti:3055` — if no output, it isn't started.
2. Restart it:
   - **NPM:** `npx @bienhoang/vibe-designing-tunnel`
   - **Source:** `npm run socket`
3. Ask the user to close and reopen the Figma plugin.

If `join_channel` fails with a `ROLE_OCCUPIED` error, another MCP server is already connected to that channel. Use `channel_info` (or `curl http://localhost:3055/channels`) to inspect who's connected. The user needs to disconnect the other MCP client or use a different channel name.

If the issue persists after these steps, direct the user to the [Vibe Designing Discord](https://discord.gg/4XTedZdwV6) for help.

If any tool times out after a successful `join_channel`, the Figma plugin is not connected to the tunnel/relay. The timeout error will include the port and channel the MCP server is using. Ask the user to check the Vibe Designing plugin window and confirm:
- The **port** matches what MCP is using
- The **channel name** matches what MCP joined
- The plugin status shows **Connected**

## Documentation

| Document | Purpose |
|----------|---------|
| [Project Overview & PDR](./docs/project-overview-pdr.md) | Vision, goals, requirements, success metrics |
| [Codebase Summary](./docs/codebase-summary.md) | File structure, modules, 50+ tool reference |
| [Code Standards](./docs/code-standards.md) | Conventions, patterns, tool registration, error handling |
| [System Architecture](./docs/system-architecture.md) | Layers, data flow, state management, security |
| [Deployment Guide](./docs/deployment-guide.md) | Setup (npm, source, Docker), troubleshooting |
| [Project Roadmap](./docs/project-roadmap.md) | Current status, phases, feature priorities |

## Common Tasks

**Setup for first time:**
→ [NPM Setup](#option-1-npm-zero-download) | [Build from Source](#option-2-build-from-source)

**Understand the architecture:**
→ [System Architecture](./docs/system-architecture.md)

**Add a new tool:**
→ [Code Standards: Tool Registration Pattern](./docs/code-standards.md#tool-registration-pattern)

**Troubleshoot connection:**
→ [Troubleshooting](#troubleshooting)

**View all tools & capabilities:**
→ [Codebase Summary: Tool Categories](./docs/codebase-summary.md#tool-categories-50-tools)

## Status

**Version:** 0.1.1 (Stable, Production Ready)

**Features:** 50+ design tools, WCAG linting, design tokens, real-time sync

**Coverage:** Creation, modification, querying, design systems, quality assurance

**Known Limits:** Single user per channel (multi-user in roadmap Phase 3)

## Community

- **[GitHub Issues](https://github.com/bienhoang/vibe-designing/issues)** — bugs and features
- **[Discord](https://discord.gg/4XTedZdwV6)** — chat and support
- **Contributing:** See CONTRIBUTING.md (coming soon)

## License & Attribution

[MIT](./LICENSE) — Free for commercial use.

Maintained by **[bienhoang](https://github.com/bienhoang)** — bridging creators and technology.
