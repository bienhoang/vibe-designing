<div align="center">

# Vibe Designing

**Vibe Design meets Figma**

Let AI agents design directly in Figma through natural conversation. Read layouts, create components, modify styles, and build entire design systems with 50+ design tools.

[![MIT License](https://img.shields.io/badge/license-MIT-blue.svg)](./LICENSE)

</div>

---

## Table of Contents

- [How It Works](#how-it-works)
- [What You Can Do](#what-you-can-do)
- [Setup](#setup)
  - [Prerequisites](#prerequisites)
  - [Option 1: Quick Install (Recommended)](#option-1-quick-install-recommended)
  - [Option 2: Build from Source](#option-2-build-from-source)
  - [About Ports](#about-ports)
  - [Connect](#connect)
  - [Troubleshooting](#troubleshooting)
  - [Instructions for AI Agents](#instructions-for-ai-agents)
- [Documentation](#documentation)
- [Common Tasks](#common-tasks)
- [Status](#status)
- [Community](#community)
- [License & Attribution](#license--attribution)

## How It Works

```
Claude/Cursor (AI)
  ↓ MCP Protocol (stdio)
MCP Server + Embedded Relay (localhost)
  ↓ WebSocket (auto port 3055-3058)
Figma Plugin (your design file)
  ↓ Figma API
Your Design System
```

AI agents send design commands via MCP tools. The MCP server includes a built-in WebSocket relay — no separate tunnel process needed. Changes appear instantly in your design file.

## What You Can Do

| Category | Tools | Examples |
|----------|-------|----------|
| **Create** | 19 | Frames, components, text, shapes, SVG imports, boolean ops, icons |
| **Modify** | 17 | Move, resize, colors, text, effects, constraints, exports |
| **Design Systems** | 16 | Create styles, variables, collections, bindings |
| **Icons** | 3 | Search 4000+ Lucide icons, list providers, create icon components |
| **Query** | 14 | Get node info, search, selection, serialize design |
| **Quality** | 1 | Lint designs, WCAG 2.2 validation, auto-fix |
| **AI Intelligence** | 1 | Bundled design system recommendations (Python 3 required) |
| **Total** | **53+** | Cover creation to production |

See full [tool reference](./docs/codebase-summary.md#tool-categories-53-tools).

## Setup

### Prerequisites

- [Node.js](https://nodejs.org) (v18+) — required for the MCP server
- [Figma](https://figma.com) desktop or web app
- An AI coding tool with MCP support (Claude Code, Cursor, etc.)
- **Best LLM:** Claude Opus 4.6 (consistent results). GPT models less reliable.

### Option 1: Quick Install (Recommended)

One command — downloads the Figma plugin and configures your AI tool automatically. The MCP server includes a built-in WebSocket relay, so no separate tunnel process is needed.

```bash
curl -fsSL https://raw.githubusercontent.com/bienhoang/vibe-designing/main/setup.sh | bash
```

This will:
1. Download the Figma plugin and MCP server to `~/vibe-designing/`
2. Auto-configure the MCP server in detected AI tools (Claude Code, Cursor, Windsurf)

After running, import the plugin in Figma: **Plugins > Development > Import plugin from manifest...** and select the `manifest.json` path shown in the terminal output.

Then continue to [Connect](#connect).

<details>
<summary>Manual MCP configuration</summary>

If the setup script didn't detect your AI tool, add this to your MCP config (e.g. `.cursor/mcp.json`, `.claude.json`, or `.mcp.json`):

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "node",
      "args": ["~/vibe-designing/dist/mcp.cjs"]
    }
  }
}
```

To use a specific port: add `"--port=3056"` to the `args` array. Replace `~` with your actual home directory path.

</details>

---

### Option 2: Build from Source

Full source control. Clone the repo and run everything locally.

```bash
git clone https://github.com/bienhoang/vibe-designing.git
cd vibe-designing
```

#### 1. Install & build

```bash
npm install
npm run build
```

#### 2. Install the Figma plugin

1. In Figma, go to **Plugins > Development > Import plugin from manifest...**
2. Select `plugin/manifest.json` from this repo (generated by `npm run build`)
3. Run the plugin — it will show a connection panel

#### 3. Configure MCP in your AI tool

Add to your MCP config (e.g. `.cursor/mcp.json`, `.claude.json`, or `.mcp.json`):

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "node",
      "args": ["/absolute/path/to/vibe-designing/dist/mcp.cjs"]
    }
  }
}
```

Or run from source (no build step, good for development):

```json
{
  "mcpServers": {
    "Vibe Designing": {
      "command": "npx",
      "args": ["tsx", "/absolute/path/to/vibe-designing/src/server/mcp.ts"]
    }
  }
}
```

The MCP server includes an embedded WebSocket relay — no separate `npm run socket` step needed. If you need a specific port, pass `--port=3056`.

> **Note:** Replace `/absolute/path/to/vibe-designing` with the actual path to your cloned repo. Run `pwd` from the vibe-designing directory to find it. MCP clients don't set a working directory, so relative paths won't resolve.

Then continue to [Connect](#connect).

---

### About Ports

The MCP server automatically scans ports **3055–3058** and picks the first available one. The Figma plugin whitelists these same ports.

To force a specific port: pass `--port=3056` to the MCP server, or set `VIBE_DESIGNING_PORT=3056`.

If the auto-selected port differs from the default (3055), update the **port field in the Figma plugin UI** before clicking Connect. The MCP server logs which port it started on to stderr.

### Connect

1. In the Figma plugin, click **Connect** (channel defaults to `vibe-designing`)
2. In your AI tool, ask: **"Use the ping tool"** — the MCP server auto-joins the default channel
3. You should get back `pong` with your Figma document name

> **Custom channel?** Call `join_channel` with a different name before other tools. The Figma plugin channel name must match.

#### Channel rules

Each channel enforces **exactly one Figma plugin and one MCP server**. If a second plugin (or second MCP) tries to join a channel that's already occupied, it will be rejected with a clear error message. To connect a different plugin, disconnect the first one or use a different channel name.

#### Debugging connections (source build only)

```bash
curl http://localhost:3055/channels
```

Returns:
```json
{
  "vibe-designing": {
    "mcp": { "connected": true, "version": "0.1.1", "joinedAt": "2026-02-28T12:00:00.000Z" },
    "plugin": { "connected": true, "version": "0.1.1", "joinedAt": "2026-02-28T12:00:01.000Z" }
  }
}
```

If using Claude Code, the `channel_info` MCP tool returns the same data without leaving your AI tool.

### Troubleshooting

**Plugin won't connect to WebSocket**: The MCP server must be running (it includes the relay). Check that the port shown in the MCP server logs matches the Figma plugin UI.

**"Connection rejected" in plugin UI**: Another plugin is already connected to that channel. Disconnect it first or use a different channel name. Run `curl http://localhost:3055/channels` to see what's connected.

**MCP shows disconnected**: Restart your AI tool after changing MCP config. Stdio-based MCP servers can't hot-reload.

**Port already in use**: The MCP server auto-scans ports 3055–3058. If all are busy, kill the existing process (`lsof -ti:3055 | xargs kill`) or set a specific port with `--port=`.

**Setup fails**: Check your internet connection. Both `curl` and `--setup` download from GitHub Releases. If they fail, manually download from [GitHub Releases](https://github.com/bienhoang/vibe-designing/releases) and unzip to `~/vibe-designing/plugin/`.

**Version mismatch warning** (source build): The plugin and MCP server are running different versions. Run `npm run build` and restart both to resolve.

---

### Instructions for AI Agents

The section below is for AI agents setting up Vibe Designing on behalf of a user. Follow these steps in order.

#### Setup

Run the setup script to download the plugin and MCP server automatically:

```bash
curl -fsSL https://raw.githubusercontent.com/bienhoang/vibe-designing/main/setup.sh | bash
```

This installs everything to `~/vibe-designing/` and configures MCP in detected AI tools. Tell the user the full path to `manifest.json` so they can import it in Figma.

If the script fails, download the latest zip from [GitHub Releases](https://github.com/bienhoang/vibe-designing/releases), unzip to `~/vibe-designing/`, and tell the user the path.

#### Port resolution

The MCP server auto-scans ports 3055–3058 and picks the first available one. No manual port checking is needed. The server logs which port it started on.

If the user needs a specific port, pass `--port=<port>` in the MCP config args.

#### Connection verification

After the user opens the Figma plugin, it should automatically show **Connected** on the default port (3055). If the MCP server auto-selected a different port, the user will need to update the port in the plugin UI and click Connect.

1. Use the `ping` MCP tool. The MCP server auto-joins the default channel (`vibe-designing`) on first tool call.
2. Expected response: `{ status: "pong", documentName: "...", currentPage: "...", timestamp: ... }`

If `ping` returns a `pong` with a document name, the full chain is verified. Proceed with design tasks. Only call `join_channel` if the user specifies a custom channel name.

#### Troubleshooting connection issues

If the plugin shows **Disconnected**, the MCP server (which includes the relay) may not be running. Restart your AI tool to restart the MCP server.

If `join_channel` fails with a `ROLE_OCCUPIED` error, another MCP server is already connected to that channel. Use `channel_info` (or `curl http://localhost:3055/channels`) to inspect who's connected. The user needs to disconnect the other MCP client or use a different channel name.

If any tool times out after a successful `join_channel`, the Figma plugin is not connected to the relay. The timeout error will include the port and channel the MCP server is using. Ask the user to check the Vibe Designing plugin window and confirm:
- The **port** matches what MCP is using
- The **channel name** matches what MCP joined
- The plugin status shows **Connected**

## Design Intelligence (Bundled)

Design system recommendations are bundled with Vibe Designing — no separate installation needed.

### Requirements

- **Python 3** — most dev machines already have it (`python3 --version`)

### Usage

Use the `design_workflow` prompt for guided end-to-end design creation with AI-powered recommendations. The prompt instructs Claude Code to run bundled Python scripts that search 50+ styles, 97 color palettes, 57 font pairings, and 99 UX guidelines.

Without Python 3, you can still provide design specs manually to create Figma designs.

---

## Documentation

| Document | Purpose |
|----------|---------|
| [Project Overview & PDR](./docs/project-overview-pdr.md) | Vision, goals, requirements, success metrics |
| [Codebase Summary](./docs/codebase-summary.md) | File structure, modules, 50+ tool reference |
| [Code Standards](./docs/code-standards.md) | Conventions, patterns, tool registration, error handling |
| [System Architecture](./docs/system-architecture.md) | Layers, data flow, state management, security |
| [Deployment Guide](./docs/deployment-guide.md) | Setup (npm, source, Docker), troubleshooting |
| [Project Roadmap](./docs/project-roadmap.md) | Current status, phases, feature priorities |

## Common Tasks

**Setup for first time:**
→ [NPM Setup](#option-1-npm-recommended) | [Build from Source](#option-2-build-from-source)

**Understand the architecture:**
→ [System Architecture](./docs/system-architecture.md)

**Add a new tool:**
→ [Code Standards: Tool Registration Pattern](./docs/code-standards.md#tool-registration-pattern)

**Troubleshoot connection:**
→ [Troubleshooting](#troubleshooting)

**View all tools & capabilities:**
→ [Codebase Summary: Tool Categories](./docs/codebase-summary.md#tool-categories-50-tools)

## Status

**Version:** 0.5.0 (Stable, Production Ready)

**Features:** 50+ design tools, WCAG linting, design tokens, real-time sync

**Coverage:** Creation, modification, querying, design systems, quality assurance

**Known Limits:** Single user per channel (multi-user in roadmap Phase 3)

## Community

- **[GitHub Issues](https://github.com/bienhoang/vibe-designing/issues)** — bugs and features
- **Contributing:** See CONTRIBUTING.md (coming soon)

## License & Attribution

[MIT](./LICENSE) — Free for commercial use.

Maintained by **[bienhoang](https://github.com/bienhoang)** — bridging creators and technology.
